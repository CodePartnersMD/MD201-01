let Person = function(name, age, jobTitle) {
    this.name = name;
    this.age = age;
    this.jobTitle = jobTitle;
}

//Create an object constructor called Person that takes in 3 properties called Name, Age, and jobTitle

const Person

module.exports = Person